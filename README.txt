=====================================================================
Docker image for Ubuntu session
by Jean-Michel RICHER, jean-michel.richer@univ-angers.fr
=====================================================================

To build the image and run container, type:

./run.sh [username]

where username is an optional parameter which by default will be
'etud' for 'etudiant' (french word for student).

The home directory of the user will be mapped as a volume in the
subdirectory 'home' inside this folder.


